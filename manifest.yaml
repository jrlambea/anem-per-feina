apiVersion: v1
kind: Namespace
metadata:
  name: anemperfeina-dev
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: env-dev
  namespace: anemperfeina-dev
data:
  DEBUG: "True"
  SECRET_KEY: "secret"
  LOG_LEVEL: "INFO"
  POSTGRES_DB: "anemperfeina"
  POSTGRES_USER: "anemperfeina"
  POSTGRES_PASSWORD: "s3cret"
  POSTGRES_HOST: "db"
  POSTGRES_PORT: "5432"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: anemperfeina-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: redis
        image: redis/redis:stable
      - name: backend
        image: geekscat/anem-per-feina:latest
        command: ["invoke"]
        args: [
          "wait-for",
          "db:5432",
          "migrate",
          "runserver",
          "--debug",
        ]
        envFrom:
          - configMapRef:
              name: env-dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql
  namespace: anemperfeina-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
      - name: postgresql
        image: postgres:latest
        envFrom:
          - configMapRef:
              name: env-dev
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: anemperfeina-dev
spec:
  ports:
  - name: http
    targetPort: 8000
    port: 80
  selector:
    app: backend
---
apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: anemperfeina-dev
spec:
  ports:
  - name: http
    targetPort: 5432
    port: 5432
  selector:
    app: db
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: dev-anemperfeina-ingress
  namespace: anemperfeina-dev
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  # tls:
  #   - hosts:
  #     - dev.nemperfeina.cat
  #     secretName: dev-nemperfeina-cat-crt
  rules:
  - host: dev.nemperfeina.cat
    http:
      paths:
      - path: /
        backend:
          serviceName: backend
          servicePort: 80
